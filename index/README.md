# index/ —— 热层 / 事后涌现的提炼物

这里放指向 `raw/` 的"门牌号":summary、关键词、一个 bug 的复盘、把若干 session 聚成"一次比赛"的视图。本目录内容同样 gitignore,私有。

它不是对价值的有损压缩,是**定位原始记录的地址**。它可以有损、可以重建、可以推倒重来——因为真东西都在 `raw/` 里一字未少。

什么时候才长出 index:当你 `raw/` 多到"手动找不动了"。在那之前,这里空着是对的——别为还没遇到的检索问题提前建结构。
