# 归墟 · 决策台账(append-only)

> 每个非平凡设计选择一条:为什么这么做 / 否决了什么 / 何时该改。
> 随决策真发生的当下记,不回溯补叙。原始判断的证据在 raw/ 冷层与 research/。

---

## D-001 · 图书管理员是"指"不是"缩";压缩只发生在激活层 · 2026-06-14

**背景 / 触发**
两条证据逼出这条:
- n=1 实测(0 号 7ny):一个 48h 事件整段 ~104 万 token,超百万窗口,激活时**必须**有损压缩才喂得进。"永不压缩"撞物理墙。
- 反面事件(raw/2026-06-10-hslty01):同一份 7ny 上下文,错位无差别灌入一个本地项目 → confabulation,编造出一台不存在的服务器凭证。证明"选哪段喂"是**安全项**不是性能项。
- 复盘线转达 wly 对外部系统的态度:允许借鉴/兼容并蓄/取其精华去其糟粕,**绝不允许抄**;判据=主体性在接触里活没活。

**决策**
"永不压缩"与"激活必须有损压缩"不矛盾,因为它们在不同层:
- **存储层(raw + index)**:绝对永不压缩。index 是门牌号,指向一字不动的 raw。
- **激活层(use)**:被窗口物理逼着做有损投影,但该投影 (a) 临时、绝不写回替代 raw;(b) 按查询变;(c) 永远可从完好 raw 重算。

图书管理员的本职是**选哪几段 raw(指)**,压缩是读取时、临时、可重算的副产品——**永远不是"删/缩原文"**。

**否决了什么**
- 否决:在写入/存储时蒸馏成 memory/summary/skill(主流 A-MEM/ExpeL/AWM/AutoManual/EvolveR 走这条)。理由=写入时压缩不可逆、一份投影冻死、原文已毁——正是 wly 说的"抄=投降主体,归墟沦为又一个记忆压缩系统"。
- 借鉴(非抄):三个范式里"怎么判断哪段最相关"的机器——Generative Agents 的 recency×importance×relevance 打分、EvolveR 的用量评分+剪枝、MemGPT 的分层换页。只取相关性判断,弃"用摘要替代原文"。

**hslty01 补出的三条安全规格**
1. provenance 不可丢:每段投影带门牌号(来自哪个事件/项目)。hslty01 根因=来源丢失被缝合。
2. 跨事件默认隔离:喂 A 给任务 B,A 必须被围栏成"另一段过去的过程,不是你当前的事实"(use 开场白已是此防线,保留强化)。
3. 不确定时宁可少喂:喂错的多上下文主动有害,相关性存疑时偏精确率。

**何时该改**
- 若实践证明"激活临时投影 + 完好 raw 重算"在某场景下成本不可接受(比如重算太贵、raw 取用太慢),重评是否需要一层**可缓存但可失效**的派生投影(仍非写入时压缩,只是 memoization)。
- 若图书管理员的相关性判断被证明系统性选错段(像 hslty01 那样),先加 provenance/隔离硬约束,而非退回写入时压缩。
- 红线不动:任何改动都不得让存储层的 append-only/原始/永不压缩松动。这条松了,归墟就死了。

**2026-06-14 补 · 找到外部先例(强化本决策,非改动)**
观察到:已有生产级 agent 系统在它**单会话内的上下文管理**里,采用了和 D-001 同构的架构——
不可变的完整历史日志是唯一真相,"发给模型的视图"是每次按需从日志重放出来的投影(event-sourcing
的 read-time projection),压缩因此**可逆**、原文从不被替代。
意义:(1) 归墟"指不缩、原始是真相、投影可重算"不是孤立异端,严肃系统内部就这么做——验证了设计方向;
(2) 将来建图书管理员的参照范式 = event-sourcing(raw = commit log,librarian = 一个 projectView())。
边界:那套机制管的是**单会话窗口压力**,归墟要的是**跨会话经验复用**——**同机制、不同尺度**。
归墟的定位句因此清晰:把会话内的 event-sourcing 读时投影,抬到会话间的经验复用。
(该先例的具体出处与实证细节,留在本地冷层事件 raw/ 里,不进公开仓。)
